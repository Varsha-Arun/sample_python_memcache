language: python
python:
 - "2.7"
#buildimage: shippable/minv2
install: pip install --use-mirrors -r requirements.txt
script:
 - python sample.py
notifications:
 email:
  varsha@shippab;le.com
archive: true
